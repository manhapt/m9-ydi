{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'Course creation'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="button button-primary" type="submit" value="{{ 'Create'|trans }}" />
        <a class="button button-primary" href="{{ path('admin_course_index') }}">{{ 'Back to the list'|trans }}</a>
    {{ form_end(form) }}
{% endblock %}
